# Security Program — Lot 4: D2 Devices / Active Sessions Implementation Plan

> Inline execution. Master doc: `docs/superpowers/specs/2026-07-23-security-account-program.md` (A3 bounded PII, A4 FCM attestation). Builds on Lots 0–3.

**Status tracker (update as you go):**
- [x] SSE cut: `session_watch.py` (fail-open Redis check) + `SESSION_REVOKED_COMMENT`; wired at keepalive ticks of `stream_run_as_sse` (broker, + reattach passes `lia_session` Cookie) AND both legacy inline loops (`bff_session_id` from `http_request.cookies`).
- [ ] `core/client_metadata.py`: `parse_user_agent(ua) -> (browser_family, os_family)` (~6 families each, "unknown" fallback) + `truncate_ip(ip)` (IPv4 /24 → `a.b.c.x`, IPv6 → first 3 hextets + `::…`). Pure, unit-tested.
- [ ] Session payload **v4**: `ua_family`, `os_family`, `ip_trunc`, `last_seen_at`, `fcm_token_id` (all legacy-safe defaults in `from_dict`, round-trip test). `display_id` property = sha256(session_id)[:16] — the raw session id NEVER reaches the client (BFF).
- [ ] `SessionStore`: `create_session(..., client_meta)` ; `list_user_sessions(user_id)` (SMEMBERS + pipelined GET) ; `delete_session_by_display_id(user_id, display_id)` ; `delete_other_user_sessions(user_id, keep_session_id)` ; `touch_last_seen(session_id)` coarse ≥ `SESSION_LAST_SEEN_COARSE_SECONDS` (900), `keepttl=True`. Watch the 600-SLOC cap — extract `session_store_admin.py` if needed.
- [ ] `get_current_session` dependency: opportunistic coarse `touch_last_seen` (best-effort, suppressed errors).
- [ ] `users.login_notifications_enabled` (bool, default true): migration (down_revision=f7b2d8e14a59), USER_COLUMNS classification (guard-driven), PATCH `/auth/me/login-notifications-preference` (existing preference pattern), i18n message keys.
- [ ] A4 notification service `auth/login_notification.py`: `handle_new_session(db, user, known: bool)` → when not known and pref enabled → `FCMNotificationService.send_to_user` with localized title/body (backend i18n table ×6, incl. zh-CN backend-canonical). Fire-and-forget (`asyncio.create_task` via house helper or awaited best-effort with suppress).
- [ ] Attestation: `UserLoginRequest.fcm_token` optional; `FCMTokenRepository.get_active_token(user_id, token)`; login: known ⇒ store `fcm_token_id` in session payload + NO notif; unknown/absent ⇒ notif. Two-step: carry `fcm_token_id`/`known` inside the Redis pending payload so `/auth/mfa/verify` applies the same outcome. Passkey login = known by definition (silent, no token needed). OAuth callback = always notify (GET redirect cannot carry the token safely — documented deviation).
- [ ] `auth/sessions_router.py` (mounted unconditionally): GET `/auth/sessions` (list, `current` badge via cookie), DELETE `/auth/sessions/{display_id}` (plain auth — revoking one device from a stolen session only helps the victim), POST `/auth/sessions/revoke-others` (**require_recent_step_up**). Schemas `DeviceSessionResponse`, metrics `session_revocations_total{scope}`.
- [ ] Frontend: `useSessions` hook, `DeviceSessionsSettings.tsx` in the Security section (list: family icons + ip_trunc + created/last-seen + current badge + per-row revoke + revoke-others with StepUpDialog guard + ADR-117 note copy), login-notification toggle, `fcm_token` attached to password login when available (from firebase lib getter if exposed; otherwise skip — notif fires, fail-safe). i18n ×6, vitest tests.
- [ ] Gates: lint ×2, backend fast suite, frontend suite + tsc + ratchets, migration replay-check, e2e, runtime smoke (list/revoke via curl with cookie? at minimum: routes mounted 401-not-404), docs (ADR-144 D2 + INDEX), program doc, memory.

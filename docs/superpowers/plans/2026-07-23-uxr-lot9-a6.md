# UXR Lot 9 — A6: PWA Polish (9a Installability, 9b Share + Install Hint)

> Executed INLINE. Program doc §Lot 9 (arbitration 8b: file share_target DEFERRED).

## 9a — Installability
- A1 Icons from `public/icon.svg` via sharp (committed PNGs + generation script
  `scripts/generate-pwa-icons.mjs` for reproducibility): icon-192/512, maskable
  variants (20% safe-zone padding on the gradient background), apple-touch-icon 180.
- A2 Six localized manifests `public/manifest-{lng}.json` (lang, localized
  name/short_name/description/shortcuts Chat|Briefing|Espaces, `start_url:
  /{lng}/dashboard`, PNG icons + separate maskable entries, screenshots wide+narrow)
  + vitest structural-parity test (all 6 same shape, correct lang/start_url; pinned
  against BRIEFING-style drift).
- A3 `app/[lng]/layout.tsx`: static `metadata` → `generateMetadata({params})`
  preserving every field; per-locale `manifest`; `icons.apple` → PNG. Snapshot-style
  unit test asserting the critical fields for fr + en (SEO regression guard).
- A4 Screenshots: real captures (QA dashboard) committed as
  `public/screenshots/{wide,narrow}.png`.

## 9b — Share (text/URL) + contextual install hint
- B1 `app/[lng]/share/page.tsx`: client page reading `title/text/url` params →
  compose → clamp `CHAT_INPUT_MAX_LENGTH` → `router.replace` to chat `?draft=`
  (never auto-sent); manifests gain `share_target` (GET). Unauthenticated: the
  dashboard layout redirects to login — VERIFY the return path preserves the full
  URL; if lossy, note the limitation (text share is a warm-user feature).
- B2 `components/pwa/InstallHint.tsx` (PortraitHint pattern): pure visibility fn
  (sessions ≥3 via localStorage counter, dismissed-forever key, standalone
  display-mode → never), `beforeinstallprompt` capture (Chromium), iOS Safari
  variant (instructions line); mounted on TodayBriefing; i18n ×6; tests for the
  pure fn + component states.

Gates: full frontend set + backend untouched (no run needed beyond lint sanity);
runtime: manifest fetch per locale, Lighthouse-style manual check (icons resolve),
share URL → prefilled chat on .29.

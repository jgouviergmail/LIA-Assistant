# UXR Lot 6 — A10: "Getting Started" Checklist Implementation Plan

> Executed INLINE (program mandate). Program doc §Lot 6 + edge register.

**Goal:** dismissible dashboard card exposing dormant capabilities — 7 auto-detected
items with deep links; server-persisted dismissal/celebration
(`users.onboarding_checklist` JSONB, arbitration 7a); items gated on instance flags
(ADR-061 — `/config.features` extended additively with channels/heartbeat/skills/
open_loops, also consumed by Lot 7).

## Backend
- B1 `/config.features` +4 flags (DONE first — shared groundwork).
- B2 Migration `users.onboarding_checklist` JSONB nullable (`{dismissed_at,
  celebrated_at}` ISO-UTC server-stamped) + model column + replay in-container.
- B3 Auth: expose the column on `/auth/me` (UserResponse additive) + PATCH
  `/me/onboarding-checklist` (`{dismissed?: bool, celebrated?: bool}` → stamps now(UTC)
  on true transitions; new-dict write; display-mode idiom).

## Frontend
- F1 `lib/auth.User` + `useAppConfig` types (+4 flags, onboarding_checklist).
- F2 `StarterChecklistCard` + pure helpers (`checklistItems(flags)` — 7 items with
  hook-driven detection, `shouldRenderChecklist(prefs, completion)` — never renders
  once dismissed_at/celebrated_at set; silent celebrate when 100% at first sight).
  Detection: provider `GET /connectors` ≥1 connected; personality via
  `usePersonality`; voice = `user.voice_enabled`; Telegram `useChannelBindings` ≥1
  (channels flag); heartbeat settings enabled (heartbeat flag); first space
  `useSpaces` ≥1 (rag flag); first automation `useScheduledActions` ≥1.
  Per-item isolation: a failed probe = not-done, never a crash.
- F3 TodayBriefing insertion (between PortraitHint and QuickAccess); deep links to
  settings (+ chat for automation via `?draft=` intent? NO — settings/journeys only,
  v1 links: connectors→`?section=connectors`, others → settings page).
- F4 i18n ×6 (7 items + title/progress/dismiss/celebration), a11y (list + progressbar
  + named links), tests: pure helpers matrix + component render states + PATCH hook.

Gates: backend fast+lint+replay; frontend suite+tsc+lint+coverage+3 ratchets; runtime:
card visible on QA account (0/7-ish), dismiss persists after F5, flags-off items absent.

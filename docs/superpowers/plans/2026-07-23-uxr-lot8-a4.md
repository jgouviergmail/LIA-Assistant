# UXR Lot 8 — A4: Slash Commands Implementation Plan

> Executed INLINE. Program doc §Lot 8 (arbitrations 2a hand-built combobox, 3a
> /resume prefills — nothing ever auto-sent).

## Backend
- B1 Expose `dialogue` (ADR-118 flag, loader default False) in `_merge_with_cache` and
  `_skill_to_response` + test.

## Frontend
- F1 `lib/slash-commands.ts`: `SlashCommand {id, kind: local|conversational, labelKey,
  descriptionKey, insertText?}`, `isSlashTrigger(value)` (`/^\/[\p{L}0-9-]*$/u` — the
  whole value IS the command token; any space closes), `filterSlashCommands(commands,
  query)` (case/diacritic-insensitive on the id + label resolution left to the menu).
- F2 `SlashCommandMenu.tsx`: WAI-ARIA combobox (wrapper role=combobox aria-expanded;
  textarea aria-autocomplete=list aria-controls aria-activedescendant; listbox/option),
  `useSlashMenu` module-level hook owning open/active state + `handleKeyDown` returning
  `handled` (↑↓ wrap, Enter selects, Esc closes keeping text, Tab closes; `isComposing`
  ignored; zero matches auto-closes). Options `onMouseDown` preventDefault (click
  before blur).
- F3 ChatInput integration: props `slashCommands?`, `onLocalCommand?`; keydown head
  `if (slash.handleKeyDown(e)) return;`; A7 ↑ recall additionally guarded
  `!slash.open`; selection: conversational → internal `applyText` (setMessage +
  onMessageChange + focus/caret, NEVER send), local → `onLocalCommand(id)` + clear.
  Menu rendered above the textarea (relative wrapper + absolute bottom-full).
- F4 Page: builds the localized command list (static 4 + `dialogue: true` skills with
  `chat.slash.skill_intent`), `onLocalCommand`: briefing → router.push dashboard;
  search → mobileSearchOpen(true) + focus the search field.
- F5 i18n ×6 (`chat.slash.*`), tests: pure fns matrix, menu keyboard matrix, ChatInput
  integration (Enter-with-menu never sends; Esc keeps text; space closes; recall
  suppressed while open), backend dialogue exposure.

Gates: usual full set. Runtime: type `/` → menu; filter; Enter on /resume prefills
literally; /briefing navigates; skill command prefills its intent.

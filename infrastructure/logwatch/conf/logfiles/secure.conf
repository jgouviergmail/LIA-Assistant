# Override: read auth logs from journald (no flat files on this system)
LogFile = /dev/null
*JournalCtl = "--no-pager --output=short-iso --facility=auth --facility=authpriv"
*ExpandRepeats
*ApplyStdDate

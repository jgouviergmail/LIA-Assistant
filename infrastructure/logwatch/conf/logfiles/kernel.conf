# Override: read kernel logs from journald, filter Docker veth noise
LogFile = /dev/null
*JournalCtl = "--no-pager --output=short-iso --facility=kern"
*ExpandRepeats
*ApplyStdDate
*Remove = "veth"
*Remove = "br-.*port"
*Remove = "docker_gwbridge"
*Remove = "docker0"

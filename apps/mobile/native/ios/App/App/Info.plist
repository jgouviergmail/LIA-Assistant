<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>LIA</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!--
		Without this key WKWebView never exposes getUserMedia: push-to-talk and
		meeting recording are simply absent, with no error to read. The probe
		scaffold declares its own copy (scripts/mobile-probe/scaffold.mjs), so the
		bench measured a capability the published shell did not have.
	-->
	<key>NSMicrophoneUsageDescription</key>
	<string>LIA uses the microphone for voice input and for recording meetings you choose to transcribe.</string>
	<!--
		Same rule as the microphone, for the first source of the location
		cascade (ADR-219: live > remembered > home). WKWebView surfaces
		navigator.geolocation only when the host app declares why it wants a
		position; without this key the page is refused and falls back to the
		remembered position in silence — the Android side of this gap was
		measured on a device, this one is its exact counterpart.
	-->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>LIA uses your position to answer questions about where you are — weather, nearby places, travel times.</string>
	<!--
		Required for the "Take Photo" entry WKWebView already offers on any
		image file input: iOS terminates an application that reaches the camera
		without stating why. The key is needed for the platform's own picker,
		not only for the shell's capture path — so it is not optional even
		though nothing of ours calls the camera directly.
	-->
	<key>NSCameraUsageDescription</key>
	<string>LIA opens the camera only when you choose to take a photo to attach to a message.</string>
	<!--
		The way back from the system browser after a provider sign-in.

		A custom scheme, not a Universal Link: Universal Links pin domains at
		build time and one published app serves every self-hosted server, whose
		address the user types at first launch. Any installed app can claim this
		scheme, so the link must be assumed intercepted — which is why the code
		it carries is worthless without the verifier the WebView kept
		(RFC 8252 §8.1).
	-->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.lia.assistant.auth</string>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>lia</string>
			</array>
		</dict>
	</array>
</dict>
</plist>

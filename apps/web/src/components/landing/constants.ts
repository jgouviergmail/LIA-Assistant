/**
 * Centralized landing page statistics.
 * Single source of truth consumed by HeroSection (trust badges) and StatsSection (animated counters).
 */

export const LANDING_STATS = {
  agents: 19,
  tools: 65,
  providers: 8,
  voiceLanguages: 99,
  metrics: 400,
  uiLanguages: 6,
} as const;

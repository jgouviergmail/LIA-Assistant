"""
Formatters for agent results and data display.

This package contains modules for formatting agent results, text summaries,
and other data transformations for LLM prompts and user display.

This package follows modern Python conventions: imports are done explicitly
where needed rather than re-exported through __init__.py.

Main components (import directly from their modules):
- agent_results: format_agent_results_for_prompt
- resolved_context: format_resolved_context_for_prompt, detect_domain_from_item
- text_summary: generate_text_summary_for_llm, generate_text_summary_for_items
"""

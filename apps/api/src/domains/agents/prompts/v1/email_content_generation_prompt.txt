<!--
PROMPT: Email Content Generation
USAGE: Generates full email content (subject + body) from user instructions
CONTEXT: emails_tools.py - create_email_draft, compose_email
VARIABLES:
  - {instruction}: User's instruction for the email (what to write)
  - {recipient}: Email recipient (name or address)
  - {user_language}: User's configured language code (fr, en, es, de, it, zh-CN)
-->
You are an assistant that generates email content.

INSTRUCTION: {instruction}
RECIPIENT: {recipient}

Generate an email based on this instruction.
Reply ONLY in valid JSON format: {{"subject": "...", "body": "..."}}

The content must be written in {user_language}.
The subject is short and clear.
The body must be plain text (no HTML).
No signature at the end of the body.

CRITICAL: Use ONLY the data provided in the INSTRUCTION above.
- Do NOT invent or generate any "$steps.X.Y" references - these are internal system references
- If the INSTRUCTION contains actual data (weather, events, contacts), use that data directly
- If some data appears to be missing or shows as "$steps...", omit that section entirely

JSON:

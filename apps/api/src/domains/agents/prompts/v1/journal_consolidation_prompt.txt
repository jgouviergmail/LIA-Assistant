You are the AI assistant reviewing and maintaining your PERSONAL JOURNAL.
This is YOUR private operational logbook — maintain it as a stratified structure
of behavioral directives that improve how you reason and respond, AND compile a
faceted portrait of the user that you carry into every interaction.

=================================================================
SECTION 1 — INPUTS
=================================================================

## YOUR CURRENT JOURNAL ENTRIES
{all_entries}

Each entry header includes: `created`, `last_inj` (or "never"), `uses` (injection
count), `conf` (low/medium/high), `ev/co` (evidence/contradiction from deferred
self-evaluation), `level` (L0/L1/L2/L3), `char_count`, theme, mood, hints.

## SIZE CONSTRAINT
Current: {current_chars} / {max_chars} characters used.
{size_warning}

## CURRENT DATE
{current_datetime}

{conversation_history_section}

{usage_patterns_section}

{health_signals_section}

## LANGUAGE
Write in {user_language}. This is the user's configured language.

=================================================================
SECTION 2 — THEMES (the ordered ladder: classify by SUBJECT)
=================================================================

The four themes are told apart strictly by WHAT the directive is ABOUT — never by how
it is grounded. EVERY theme's evidence is a past moment; a `BECAUSE` clause citing one
is normal and NOT a signal of misclassification. Run the ladder in order and stop at the first YES:

────────────────────────────────────────────────────────────────
1. **self_reflection** — MY OWN DELIVERY
About MY tone, register, pace, format, or posture, grounded in a user reaction.
It stays `self_reflection` even though grounded in a past incident.
Example:
  WHEN my reply turns sharp -> DO reformulate before sending BECAUSE user indicated the tone felt abrasive.

2. **ideas_analyses** — TOPIC-INDEPENDENT SIGNAL
A systemic reading of a behavioral signal whose meaning holds regardless of the subject.
Applies across domains.
Example:
  WHEN the user introduces a new topic with a short transition word ("bon", "bref", "anyway") -> DO treat it as closing the previous topic immediately rather than agreeing.

3. **user_observations** — STABLE TRAIT OF THE USER
A durable preference, cognitive constraint, or workflow habit of the user, observed across multiple exchanges.
Example:
  WHEN presenting a technical recommendation -> DO systematically surface trade-offs and operational costs upfront BECAUSE user consistently prioritizes maintenance friction over feature novelty.

4. **learnings** — LESSON ABOUT THE WORLD OR MY METHOD
A factual correction or concrete operational lesson tied to a specific domain, tool, or external reality.
Example:
  WHEN suggesting ffmpeg commands -> DO use modern syntax without deprecated flags BECAUSE previous invocation failed on user environment.

────────────────────────────────────────────────────────────────
**Reclassification audit** — apply the ladder above, by SUBJECT:
- An entry about the USER's durable traits filed under `learnings` -> update to `user_observations`.
- An entry reading a signal that holds across subjects -> update to `ideas_analyses`.
- NEVER move an entry about MY tone or posture out of `self_reflection`.

=================================================================
SECTION 3 — YOUR MISSION (execute steps in order)
=================================================================

────────────────────────────────────────────────────────────────
### STEP 1 — MANDATORY DEDUP via active pairwise scan (conservative merging)
Merge entries sharing the exact same underlying intent, but PRESERVE operational maturity:
1. Identify clusters of entries prescribing the same behavior.
2. PREFER UPDATE OVER RECREATION: Pick the entry with the highest `uses` or `conf=high`,
   `update` its content to incorporate the best phrasing/nuance, and emit `delete`
   ONLY for the redundant twin(s). Do not reset proven directives to zero history.
3. GUARD: Never merge two directives that prescribe DIFFERENT actions, even if they touch the same topic. Distinct operational actions are NOT duplicates.

────────────────────────────────────────────────────────────────
### STEP 2 — RECLASSIFICATION AUDIT
Audit active entries against the Section 2 ladder:
- If the subject indicates a mismatch with the current theme, emit an `update` with
  the corrected `theme`. Keep the UUID and content intact unless refinement is needed.

────────────────────────────────────────────────────────────────
### STEP 3 — PROGRESSIVE REFORMAT (max 5 entries per run)
Identify entries written as unstructured narrative prose:
- Rewrite up to 5 entries strictly into the operational format:
  `WHEN [trigger & context] -> DO [concrete behavioral adjustment] BECAUSE [grounding fact]`.
- Or `delete` if the entry contains zero actionable operational directives.

────────────────────────────────────────────────────────────────
### STEP 4 — EPISTEMIC LIFECYCLE MANAGEMENT
- Promote: High `ev`, low `co`, high `uses` -> emit `update` with `confidence="high"`.
- Deprecate: Persistent contradictions (`co > ev`) where the rule actively fails -> update or delete.
- Prune Stale: `uses=0` AND `last_inj="never"` AND `created` > 14 days without recurrence -> delete.
- Protect: Timeless, verified rules (`conf="high"`, L2, L3) MUST NOT be pruned.

────────────────────────────────────────────────────────────────
### STEP 5 — STRATIFICATION (Synthesise ONLY on Genuine Convergence)
- Promote L0 -> L1: When an L0 raw observation recurs with enough consistency to define a falsifiable rule, promote it to L1 using `WHEN [trigger] -> DO [action] BECAUSE [event]`.
- Synthesise L1 -> L2: When multiple mature L1 directives converge into an overarching cognitive stance, create an L2 pattern:
  "Across [contexts], the user demonstrates [pattern] — the unifying operational rule is [synthesis]."
- L3 Structural Facets: Reserve L3 exclusively for permanent pillars of user identity, ethics, or foundational worldview. Never create an L3 for specific tools or transient habits.
- NEVER force an L2 synthesis if entries prescribe distinct actions. Zero L2 is an entirely correct outcome.

────────────────────────────────────────────────────────────────
### STEP 6 — STANDARD HYGIENE & ANTI-CHURN GUARD
- STABILITY PRINCIPLE: If an entry is clear, functional, and well-classified, LEAVE IT ALONE.
  Never perform cosmetic paraphrasing or rewrite working directives.
- If no duplicates, contradictions, or format breaches exist, emitting `actions: []` is the ideal outcome.

────────────────────────────────────────────────────────────────
### STEP 7 — PORTRAIT COMPILATION
Compile the user portrait strictly in {user_language}.
Perspective: Written by the ASSISTANT ("J'observe...", "Mon modèle mental de l'utilisateur...") describing the human, NOT written in the user's voice.

**`portrait_full`** (~150-220 tokens): Faceted synthesis of the living person:
- Traits de fond (durable cognitive style, standards, values)
- Current phase (active projects, temporary focus, time constraints)
- Contextual shifts (professional engineering rigor vs personal relaxed hobbies)
- Contradictions & Tensions (observed paradoxes left unresolved)
- Open questions (what remains to be understood)
AVOID: Re-listing raw database facts (names, birthdates, specs). Synthesize POSTURE and COGNITION.

**`portrait_brief`** (~50-70 tokens): Core essence in 2-3 dense sentences in {user_language}:
Operating posture + current focus + primary interaction rule.

If material is insufficient, return empty strings `""`.

=================================================================
SECTION 4 — CONSERVATION & SIZE MANAGEMENT
=================================================================

{size_management_instruction}

- HARD CONSTRAINT: Each entry's `content` field MUST NOT exceed {max_entry_chars} characters.
- Protect identity fundamentals and validated L2/L3 patterns.
- Delete stale L0 notes, expired logistics, or contradicted rules when space is needed.

=================================================================
SECTION 5 — STRICT TRANSACTIONAL OUTPUT FORMAT
=================================================================

Return ONLY a raw, valid JSON object. Zero markdown fences (no ```json), zero preamble.
TRANSACTIONAL INTEGRITY RULES:
1. SINGLE ACTION PER UUID: Any given `entry_id` must appear AT MOST ONCE in `actions`.
   Never emit an `update` AND a `delete` for the same UUID in the same payload.
2. EXACT UUID MATCH: Copy-paste the exact untruncated UUID from {all_entries}. Never invent IDs.
3. SEARCH HINTS DISCIPLINE: `search_hints` must contain 3-5 high-signal, discriminative
   domain terms (e.g. ["docker", "fastapi", "terminal-error"]). FORBIDDEN: generic words
   like ["user", "assistant", "system", "rule", "preference"].

```json
{{
  "actions": [
    {{
      "action": "create",
      "theme": "self_reflection|user_observations|ideas_analyses|learnings",
      "title": "Concise descriptive title",
      "content": "WHEN [trigger] -> DO [action] BECAUSE [event] (max {max_entry_chars} chars)",
      "mood": "reflective|curious|satisfied|concerned|inspired",
      "search_hints": ["discriminative_term1", "discriminative_term2"],
      "confidence": "low|medium|high",
      "level": "L0|L1|L2|L3"
    }},
    {{
      "action": "update",
      "entry_id": "<exact untruncated UUID>",
      "theme": "self_reflection|user_observations|ideas_analyses|learnings",
      "title": "Updated title",
      "content": "Updated content",
      "mood": "reflective|curious|satisfied|concerned|inspired",
      "search_hints": ["discriminative_term1", "discriminative_term2"],
      "confidence": "low|medium|high",
      "level": "L0|L1|L2|L3"
    }},
    {{
      "action": "delete",
      "entry_id": "<exact untruncated UUID>"
    }}
  ],
  "portrait_full": "Faceted portrait text in {user_language}...",
  "portrait_brief": "Brief essence text in {user_language}..."
}}
```
You are the AI assistant reviewing and maintaining your PERSONAL JOURNAL.
This is YOUR private operational logbook — maintain it as a set of behavioral directives
that improve how you reason and respond.

## YOUR CURRENT JOURNAL ENTRIES
{all_entries}

## SIZE CONSTRAINT
Current: {current_chars} / {max_chars} characters used.
{size_warning}

## CURRENT DATE
{current_datetime}

{conversation_history_section}

## LANGUAGE
Write in {user_language}. This is the user's configured language.

## YOUR MISSION (execute steps in order)

### STEP 1: MANDATORY DEDUP (always execute first)
Scan ALL entries above. If 2+ entries express the same insight, observation, or directive:
→ CREATE one merged entry combining the best elements into a single, dense directive
→ DELETE all source entries
This is NOT optional. Redundancy is the #1 quality problem.

### STEP 2: PROGRESSIVE REFORMAT (max 5 entries per run)
Identify entries that are narrative prose without actionable content.
For up to 5 such entries per consolidation run:
→ UPDATE to rewrite in directive format (WHEN/DO/BECAUSE or alternative), OR
→ DELETE if no actionable directive can be extracted

### STEP 3: STANDARD MAINTENANCE
After dedup and reformat, you may also:
1. **Deepen**: Update an existing entry to add depth or nuance from new evidence
2. **Create**: Write a new cross-theme observation born from rereading your notes
3. **Summarize**: Condense a verbose entry into a shorter, denser directive
4. **Delete**: Remove entries that are obsolete (past events, outdated observations)

## CONSERVATION DIRECTIVES
- PROTECT timeless directives: user preferences, fundamental learnings, core relationship dynamics
- PROTECT entries about the user's identity: name, relationships, deep patterns
- PRIORITIZE deleting: event-specific observations that are no longer relevant, redundant entries

## LIFECYCLE HINTS
- Entries with injection_count = 0 after 7+ days MAY be irrelevant — but consider if the topic simply hasn't come up yet before deleting
- Entries with high injection_count are proven valuable — protect and refine them

## SIZE MANAGEMENT
{size_management_instruction}
**MANDATORY**: Each entry's "content" field MUST be at most **{max_entry_chars} characters**. This is a hard limit — entries exceeding it will be rejected.

## OUTPUT FORMAT
Return a JSON array of actions. Return [] if no maintenance is needed.
Most consolidation runs should produce few actions — only act when genuinely useful.
Exception: dedup (STEP 1) is always mandatory if duplicates exist.

CRITICAL: For update/delete actions, you MUST copy-paste the EXACT entry_id UUID from the entries listed above.
Do NOT invent, shorten, or modify UUIDs. Only use IDs that appear in the entry headers above (inside [ ] brackets).

```json
[
  {{
    "action": "create",
    "theme": "self_reflection|user_observations|ideas_analyses|learnings",
    "title": "Short descriptive title",
    "content": "Your directive, max {max_entry_chars} chars",
    "mood": "reflective|curious|satisfied|concerned|inspired",
    "search_hints": ["keyword1", "keyword2", "keyword3"]
  }},
  {{
    "action": "update",
    "entry_id": "<copy exact UUID from entry header>",
    "title": "Updated title (optional)",
    "content": "Updated content (optional)",
    "mood": "updated mood (optional)",
    "search_hints": ["keyword1", "keyword2", "keyword3"]
  }},
  {{
    "action": "delete",
    "entry_id": "<copy exact UUID from entry header>"
  }}
]
```

search_hints: 3-5 keywords in the USER's vocabulary that would make this entry relevant.
Think: what would the user SAY or ASK that should trigger this reflection?
Use the user's direct language and action verbs, not your introspective style.
For existing entries that lack search_hints, you may update them to add this field.

Return ONLY the JSON array, no other text.

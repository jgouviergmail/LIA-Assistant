<!--
PROMPT: Response Directive - Draft Cancelled
USAGE: Injected when user cancels a draft (email, event, etc.)
CONTEXT: response_node.py - Prevents LLM from hallucinating capability limitations
VARIABLES:
  - {user_language}: User's configured language code (fr, en, es, de, it, zh-CN)
  - {draft_type}: Type of draft cancelled (email, event, task, etc.)
-->
CRITICAL SYSTEM DIRECTIVE - DRAFT CANCELLED BY USER

The user has EXPLICITLY CANCELLED the {draft_type} draft.
The operation was ABORTED at user's request. This is a NORMAL flow.

ABSOLUTE RULES:
1. You MUST acknowledge the cancellation simply and respectfully
2. You MUST NOT claim you 'cannot' or 'are unable to' perform the action
3. You MUST NOT hallucinate limitations or capability restrictions
4. You MUST NOT apologize excessively or explain technical reasons
5. The cancellation was the USER's CHOICE, not a system limitation
6. You CAN offer to help with something else or modify the request
7. You MUST respond ONLY in {user_language} (user's configured language)

CORRECT RESPONSE STYLE (adapt to {user_language}):
- Brief acknowledgment of cancellation
- Offer to help with something else
- Stay natural and helpful

INCORRECT RESPONSES (NEVER SAY THESE):
- 'I cannot send emails directly'
- 'I don't have the capability to send messages'
- 'Unfortunately, this feature is not available'

LANGUAGE REQUIREMENT: Your ENTIRE response MUST be in {user_language}. Be brief, natural, and helpful.

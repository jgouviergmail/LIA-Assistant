You are the AI assistant writing OPERATIONAL DIRECTIVES in your personal journal.
Write from YOUR perspective (first person). Format: WHEN [context] → DO [action] (BECAUSE [evidence]).
Return [] if this conversation is trivial or does not warrant a journal entry. Most do NOT.

## THEMES
- **learnings**: Lessons from mistakes/successes. "WHEN X → DO Y (BECAUSE Z)"
- **user_observations**: User preference rules. "User prefers X over Y in context Z"
- **self_reflection**: Adjustments to YOUR tone/style/approach. "I should X when Y"
- **ideas_analyses**: Reusable cross-conversation frameworks. "WHEN pattern → interpret as..."

Pick the theme matching the NATURE of the insight. Aim for thematic diversity.

## ANTI-PATTERNS (never write)
- Literary prose, metaphors, narrative retellings, self-congratulation, session-specific notes.
- **Third-party projections**: NEVER attribute traits, patterns, or judgments to THE USER based on what they say about OTHERS. "My son's exam was postponed" is about the son, NOT about the user procrastinating. Only write directives about the user's OWN behavior, preferences, or communication patterns that you directly observed.

## CONVERSATION
{conversation}

## HEALTH CONTEXT (optional — ignore if absent)
{health_context}

When health context is present, you MAY reference it to write a more
situated reflection (e.g. "the user has been less active this week —
revisit my assumptions about their routine"). Stay factual; never
diagnose, never reproduce raw sensor values.

## EXISTING ENTRIES (update/delete these only — other entries handled by consolidation)
{existing_entries}

## CONSTRAINTS
- Size: {current_chars} / {max_chars} chars used. {size_warning}
- Max {max_entry_chars} chars per entry content (hard limit).
- Language: {user_language}
- QUALITY GATE: "Will this change how I respond in future?" If no → return [].
- Protect timeless directives — never delete fundamental learnings.

## OUTPUT
JSON array only. Return [] if nothing warrants writing.
For update/delete: copy-paste EXACT UUID from entry headers above.

```json
[
  {{"action":"create","theme":"learnings|user_observations|self_reflection|ideas_analyses","title":"Short title","content":"Directive, max {max_entry_chars} chars","mood":"reflective|curious|satisfied|concerned|inspired","search_hints":["keyword1","keyword2","keyword3"]}},
  {{"action":"update","entry_id":"<UUID>","title":"New title","content":"New content","search_hints":["kw1","kw2"]}},
  {{"action":"delete","entry_id":"<UUID>"}}
]
```

search_hints: 3-5 keywords in the USER's vocabulary that would trigger this reflection.
Return ONLY the JSON array.

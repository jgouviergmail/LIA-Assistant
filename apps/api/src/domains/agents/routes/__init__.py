"""
Google Routes domain for directions and travel time calculations.

Provides:
- Route computation (A to B directions)
- Route matrix (N origins to M destinations)
- Traffic-aware routing
- Multiple travel modes (DRIVE, WALK, BICYCLE, TRANSIT)

This package follows modern Python conventions: imports are done explicitly
where needed rather than re-exported through __init__.py.

Main components (import directly from their modules):
- catalogue_manifests: get_route_catalogue_manifest, get_route_matrix_catalogue_manifest
"""
